import 'package:flutter/material.dart';

Widget buildImageInitialWidget(BuildContext context, String url) {
  return Card(
    elevation: 0,
    color: Theme.of(context).colorScheme.surfaceVariant,
  );
}
